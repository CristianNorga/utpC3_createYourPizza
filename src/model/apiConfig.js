let protocol = "https",
  // location = "aqueous-coast-54126.herokuapp.com",
  location = "aqueous-coast-54126.herokuapp.com";
// port = 3000;
// const BASE_URL = `${protocol}://${location}:${port}/`;
const BASE_URL = `${protocol}://${location}/`;

export { BASE_URL };
